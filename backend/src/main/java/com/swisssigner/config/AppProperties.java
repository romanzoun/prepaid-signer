package com.swisssigner.config;

/**
 * Placeholder – superseded by SwisscomSignProperties and StripeProperties.
 * Kept to avoid file rename; not registered as a Spring bean.
 */
class AppProperties {
    // intentionally empty
}
